[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="openscale"
version="0.1.0"
description="Hardware-aware AI compute runtime"
requires-python=">=3.9"
[tool.setuptools.packages.find]
where=["python"]

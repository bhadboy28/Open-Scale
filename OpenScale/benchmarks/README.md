# Benchmarks

The benchmark executable measures the native FP32 matrix multiplication path. Future benchmarks should compare baseline implementations against OpenScale and report latency, throughput, peak RAM, load time, cache behavior, and CPU/GPU utilization.
